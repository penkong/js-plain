function similar(a: number[], b: number[]): boolean {
  if (a.toString() === b.toString()) {
    return true;
  }
  const c: number[] = [];
  let d: number[] = [];
  return Number;
}

console.log(similar([1, 3, 4, 5], [3, 5, 4, 1])); // true;
console.log(similar([1, 3, 4, 5], [3, 5, 6, 1])); // true;
console.log(similar([1, 3, 4, 5], [3, 5, 1, 1])); // true;
