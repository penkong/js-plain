function add(n1: number, n2: number): number {
  // js vs ts runtime check vs devtime check
  // if (typeof n1 === 'number' && typeof n2 === 'number') {
  // }
  return n1 + n2;
}
const num1 = 3;
const num2 = 3;

const res = add(num1, num2);
// console.log(res);
